#set($subject = "$device.name: alarme")
#set($alarmKey = $event.getString('alarm'))
#set($alarmName = $translations.get("alarm${alarmKey.substring(0, 1).toUpperCase()}${alarmKey.substring(1)}"))
#set($digest = "$device.name: $alarmName em $dateTool.format('dd/MM/yyyy HH:mm', $event.eventTime, $locale, $timezone)#{if}($position.address) - $position.address#{end}")
<!DOCTYPE html>
<html>
<body>
Dispositivo: $device.name<br>
Alarme: $alarmName<br>
Hora: $dateTool.format('dd/MM/yyyy HH:mm', $event.eventTime, $locale, $timezone)<br>
Endereço: <a href="$webUrl/event/$event.id">#{if}($position.address)$position.address#{else}$position.latitude&deg;, $position.longitude&deg;#{end}</a><br>
<br>
<a href="$webUrl/settings/notifications?token=$token">Cancelar subscrição</a>
</body>
</html>
