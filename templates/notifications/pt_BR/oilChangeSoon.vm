#set($subject = "$device.name: troca de oleo em breve")
#set($digest = "Troca de oleo em breve em $device.name as $dateTool.format('yyyy-MM-dd HH:mm:ss', $event.eventTime, $locale, $timezone)")
<!DOCTYPE html>
<html>
<body>
Dispositivo: $device.name<br>
Troca de oleo em breve<br>
#if($event.attributes.oilReason)Motivo: $event.attributes.oilReason<br>#end
#if($event.attributes.oilDueKm)Km previsto: $event.attributes.oilDueKm km<br>#end
#if($event.attributes.oilCurrentKm)Km atual: $event.attributes.oilCurrentKm km<br>#end
#if($event.attributes.oilKmRemaining)Faltam km: $event.attributes.oilKmRemaining<br>#end
#if($event.attributes.oilDueDate)Data prevista: $event.attributes.oilDueDate<br>#end
#if($event.attributes.oilDaysRemaining)Faltam dias: $event.attributes.oilDaysRemaining<br>#end
Hora: $dateTool.format('yyyy-MM-dd HH:mm:ss', $event.eventTime, $locale, $timezone)<br>
Local: <a href="$webUrl/event/$event.id">#{if}($position.address)$position.address#{else}$position.latitude&deg;, $position.longitude&deg;#{end}</a><br>
<br>
<a href="$webUrl/settings/notifications?token=$token">Cancelar inscricao</a>
</body>
</html>
